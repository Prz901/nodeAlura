var VNode = require("./VNode");
var inherit = require("raptor-util/inherit");

function VText(value) {
    this.br_(-1 /* no children */);
    this.bs_ = value;
}

VText.prototype = {
    bM_: true,

    bt_: 3,

    bq_: function (doc) {
        return doc.createTextNode(this.bs_);
    },

    __: function () {
        return new VText(this.bs_);
    }
};

inherit(VText, VNode);

module.exports = VText;